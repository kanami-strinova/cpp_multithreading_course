# 📝 第一章练习：线程基础

## 练习 1.1: 创建你的第一个线程

**要求**：
- 创建 3 个线程
- 每个线程打印自己的 ID 和序号
- 使用 `join()` 等待所有线程结束

**提示**：
```cpp
std::thread t(函数名, 参数);
```

---

## 练习 1.2: 线程参数传递

**要求**：
- 创建一个函数，接收整数参数
- 用不同参数启动多个线程
- 观察参数传递方式

**思考**：
- 参数是按值传递还是引用传递？
- 如何传递引用？

---

## 练习 1.3: 线程局部存储

**要求**：
- 使用 `thread_local` 声明线程局部变量
- 验证不同线程有不同的变量副本

---

## 答案参考
查看 `solutions/01_basics_solutions.cpp`
